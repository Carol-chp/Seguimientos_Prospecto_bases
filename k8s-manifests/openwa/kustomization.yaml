apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - configmap.yaml
  - secret.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml

images:
  - name: registry.registry.svc.cluster.local:5000/openwa-api
    newTag: latest
